name: Nostale Bot Exe Derleyici

on:
  push:
    branches: [ "main" ]
  workflow_dispatch: # İstediğin zaman GitHub paneli üzerinden manuel başlatabilmek için

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Depoyu Klonla
      uses: actions/checkout@v4

    - name: Python Kur
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Gerekli Kütüphaneleri Yükle
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller keyboard pyautogui opencv-python numpy customtkinter

    - name: PyInstaller ile EXE Oluştur
      run: |
        # --onefile: Tek bir exe oluşturur
        # --noconsole: Arkadaki siyah cmd ekranını gizler, sadece CustomTkinter menüsü açılır
        # --add-data: Görselleri exe içine gömer (Windows için aralarına ; koyulur)
        pyinstaller --onefile --noconsole --add-data "olta_sablonu.png;." --add-data "olta_sablonu2.png;." --name="Nostale_Balik_Botu" main.py

    - name: Oluşan EXE'yi Artefakt Olarak Yükle
      uses: actions/upload-artifact@v4
      with:
        name: Nostale_Balik_Botu_EXE
        path: dist/Nostale_Balik_Botu.exe
        
